# $Id$
package KeywordsAsTags::L10N::ja;

use strict;
use base 'KeywordsAsTags::L10N';
use vars qw( %Lexicon );

our %Lexicon = (
    q(KeywordsAsTags allows you to convert 'entry keywords' to MT3.3+ tags when posting an entry with keywords. It is convenient for you to post tagged entries from a blog client application which does not support 'tag' feature.) =>
	q(KeywordsAsTagsを使うと、キーワード付きのエントリーを投稿した際に自動的にそのキーワードをMovable Typeの「タグ」に変換することができます。この機能は、「タグ」をサポートしていないブログクライアントアプリケーションから「タグ付きエントリー」を投稿したい場合に便利です。),
);

1;
